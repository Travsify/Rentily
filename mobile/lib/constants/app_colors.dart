import 'package:flutter/material.dart';

class AppColors {
  // 1. Core Base Color 1: Royal Blue (Trust, Institutional Security, Escrow)
  static const Color primary = Color(0xFF1E40AF); // Deep Royal Blue
  static const Color primaryLight = Color(0xFF2563EB); // Vibrant Electric Royal Blue
  static const Color primaryDark = Color(0xFF172554); // Dark Navy Blue

  // 2. Core Base Color 2: Electric Sunset Orange (Wealth, Vitality, Property Keys, Actions)
  static const Color accentOrange = Color(0xFFFF6B00); // Vibrant Fintech Orange
  static const Color accentGold = Color(0xFFFF6B00); // Secondary Orange Accent
  static const Color accentGoldDark = Color(0xFFEA580C);

  // 3. Canvas: Clean Crisp White & Modern Neo-Fintech Surfaces
  static const Color backgroundDark = Color(0xFFF8FAFC); // Pristine Off-White Canvas
  static const Color surfaceDark = Color(0xFFFFFFFF); // Pure Crisp White Card
  static const Color cardDark = Color(0xFFFFFFFF); // Elevated Pure White Surface
  static const Color borderDark = Color(0xFFE2E8F0); // Subtle Border Divider

  // 4. High-Contrast Typography
  static const Color textPrimary = Color(0xFF0F172A); // Midnight Slate Navy
  static const Color textSecondary = Color(0xFF475569); // Muted Slate Gray
  static const Color textMuted = Color(0xFF94A3B8); // Subtle Slate Gray

  // Status & Utility
  static const Color success = Color(0xFF10B981);
  static const Color warning = Color(0xFFFF6B00);
  static const Color error = Color(0xFFEF4444);
  static const Color info = Color(0xFF2563EB);
}
